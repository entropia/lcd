#include <stdio.h>
#include "output.h"

#define MIN(a,b) ((a)<(b)?(a):(b))

void main() {
    int curline=0;

    if (argc<2) {
        printf("geh weg");
        exit(-42);
    }

    while(1) {
        for 
        scrollup(1);
        writescreen();
        usleep(150000);
    }
}

